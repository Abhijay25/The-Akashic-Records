import "~style.css"

// Popup shell — teammate owns this file.
// Rendering a placeholder so the project builds during engine-only development.

export default function Popup() {
  return (
    <div className="w-[400px] min-h-[500px] bg-gray-950 text-gray-100 p-4 flex items-center justify-center">
      <p className="text-gray-500 text-sm">The Akashic Records — loading...</p>
    </div>
  )
}
