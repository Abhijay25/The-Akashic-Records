// Speedreader tab — teammate owns this file.
// Renders a Book Chapter as clean markdown prose.
// URL params: ?bookId=<id>&entryId=<id>

export default function Reader() {
  return (
    <div className="min-h-screen bg-gray-950 text-gray-100 p-8 flex items-center justify-center">
      <p className="text-gray-500 text-sm">Speedreader — loading...</p>
    </div>
  )
}
